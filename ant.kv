BoxLayouts:

<BoxLayouts>:
    orientation: "vertical"

    Button:
        text: "^\n^"
        size_hint: 1, .25
        pos_hint: {'right':1, 'top': 1}

    BoxLayout:
        size_hint: 1, .25
        orientation: "horizontal"

        Label:
            text: '1'
            size_hint: .33, 1
            pos_hint: {'x':0, 'top': 1}

        Label:
            text: "2"
            size_hint: .34, 1
            pos_hint: {'x':.33, 'top': 1}

        Label:
            text: "3"
            size_hint: .33, 1
            pos_hint: {'right':1, 'top': 1}

    Image:
        source: "images/anthill_tunnels.png"
        allow_stretch: True
        keep_ratio: False
        fit_mode: "contain"

